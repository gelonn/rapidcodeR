% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{set_api_specs}
\alias{set_api_specs}
\title{Set API Specifications for AI Models}
\usage{
set_api_specs(provider, model, temp, api_key, multi_response = FALSE)
}
\arguments{
\item{provider}{Character. The AI provider to use, either "OpenAI" or "Groq".}

\item{model}{Character. The specific model to use (e.g., "gpt-4", "gpt-3.5-turbo", "llama-3.3-70b-versatile").}

\item{temp}{Numeric. Temperature parameter controlling randomness (0-1). Lower values make output more deterministic.}

\item{api_key}{Character. The API key for the specified provider.}

\item{multi_response}{Logical. If TRUE, allows AI to return multiple rows per input (only works with n_post=1).
If FALSE (default), AI must return exactly one row per input.}
}
\value{
Character. A confirmation message.
}
\description{
This function sets the API specifications including provider, model, temperature,
and API key for AI model interactions. It replaces the need for separate
set_api_keys calls and allows for more flexible configuration.
}
\details{
This function stores the API specifications in the package's internal environment
and sets the appropriate environment variables for API authentication. The
specifications are automatically retrieved by processing functions.
}
\examples{
\dontrun{
# Set OpenAI specifications
set_api_specs(
  provider = "OpenAI",
  model = "gpt-4",
  temp = 0.7,
  api_key = "your_openai_key"
)

# Set GROQ specifications
set_api_specs(
  provider = "Groq",
  model = "llama-3.3-70b-versatile",
  temp = 0.3,
  api_key = "your_groq_key"
)
}

}
\seealso{
\code{\link[=set_coding_instruction]{set_coding_instruction()}}, \code{\link[=set_parameters]{set_parameters()}}
}
